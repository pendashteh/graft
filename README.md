# graft
CLI CMS

```
Available tasks:
 - copy      # @arg file @does copy file content into a new leaf
 - done      # 
 - edit      # 
 - git       # @arg git command @does initiates or executes git commands at datadir__
 - headings  # @arg file @arg range=1, @prints list of heaings
 - help      # Usage: graft help [task]
 - here      # 
 - list      # 
 - loop      # 
 - move      # @arg pattern @arg destination:dir @does move the leaf to destinatoin directory.
 - new       # 
 - search    # 
 - todo      # 
 - tree      # @arg [pattern=*] [range=1,] @prints tree of leaves and their headlines @return null
```
